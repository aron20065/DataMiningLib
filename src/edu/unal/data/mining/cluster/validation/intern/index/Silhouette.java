package edu.unal.data.mining.cluster.validation.intern.index;

import java.util.List;

import edu.unal.data.mining.DataSet;
import edu.unal.data.mining.cluster.Cluster;

public class Silhouette {
	private static Cluster clusterMin = null;
	public static double calculate(List<Cluster> clusters) {
		return 0.0;
	}
	/**
	 * distancia promedio de x a todos los demás puntos en el
	mismo clúster.
	 * @param dataSet
	 * @return
	 */
	private static double a(DataSet dataSet,List<DataSet> dataSets) {
		return 0.0;
	}
	private static double b(List<DataSet> dataSet,List<Cluster> clusterList) {
		return 0.0;
	}
}
